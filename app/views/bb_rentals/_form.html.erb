<div class="d-flex justify-content-center">
  <div class="card" style="width: 400px;">
    <div class="card-body">
      <h5 class="card-title"></h5>

<%= form_with(model: @bb_rental) do |form| %>
  <% if rental.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bb_rental.errors.count, "error") %> prohibited this rental from being saved:</h2>

      <ul>
        <% bb_rental.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :bb_client_id, "Cliente", style: "display: block" %>
    <%= form.collection_select :bb_client_id, BbClient.all, :id, :name, prompt: "Seleccione un cliente" %>
  </div>

  <div>
    <%= form.label :bb_movie_id, "Película", style: "display: block" %>
    <%= form.collection_select :bb_movie_id, BbMovie.all, :id, :name, prompt: "Seleccione una película" %>
  </div>

  <div class="d-flex justify-content-between">
          <%= form.submit "Guardar", class: "btn btn-primary" %>
          
        </div>
      <% end %>

    </div>
  </div>
</div>